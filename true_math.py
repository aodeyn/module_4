from math import inf
def divide(first, second):
    #first = input('Введите делимое : ')
    #second = input('Введите делитель : ')
    if second != 0:
        div = first / second
        return div
    else:
        return inf